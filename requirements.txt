# Core
python-docx>=0.8.11
PyPDF2>=3.0.0
python-pptx>=0.6.21
markdownify>=0.11.6
ebooklib>=0.18
chardet>=5.0.0
beautifulsoup4>=4.12.0

# OCR (at least one required; auto priority: paddleocr > easyocr > tesseract)
# paddlepaddle>=3.2 & paddleocr>=3.3  # Best Chinese accuracy. NOTE: paddle 3.3.x on
#                                     # Windows CPU requires enable_mkldnn=False (already set in ocr_tool.py)
# easyocr>=1.7.0          # Alternative: deep-learning OCR via torch
# pytesseract>=0.3.10     # Alternative: lightest, requires system Tesseract

# PDF-to-image (for scanned PDF OCR)
pdf2image>=1.16.0       # Requires poppler system package

# Output formats
fpdf2>=2.7.0            # PDF output — pure Python, zero system deps, works everywhere (primary)
weasyprint>=60.0        # PDF output — higher quality, needs GTK3 on Windows/Linux (optional upgrade)
jinja2>=3.1.0           # For HTML templating

# Development
pytest>=7.0.0           # Testing
